kind: Patchwork

includes: 
- ../ingress.yml

patches: 
- path: metadata
  match:
   bleh: 1
   annotations:
     kubernetes.io/ingress.class: ng.*
  # there should be separate path for patch (?)
  # maybe behavior for arrays should vary: add item, merge items, spread update to all items?
  patch:
    dupa: 1